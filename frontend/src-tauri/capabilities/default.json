{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability para la ventana principal de Bandes",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "dialog:default",
    "fs:allow-write-file",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/backend-api",
          "sidecar": true
        }
      ]
    }
  ]
}