'use client';

export default function BarrasPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-[var(--hud-bg-deepest)]">
      <p className="text-sm font-mono uppercase tracking-[0.2em] text-[var(--hud-text-dim)]">
        Barras — En construcción
      </p>
    </div>
  );
}
